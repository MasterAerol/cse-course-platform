import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router'
import { fetchSubjectAssessmentResult, type SubjectAssessmentResult } from '../lib/api'
export function SubjectAssessmentResultPage() { const { attemptPublicId = '' } = useParams(); const [result, setResult] = useState<SubjectAssessmentResult | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { const controller = new AbortController(); fetchSubjectAssessmentResult(attemptPublicId, controller.signal).then(setResult).catch((value: unknown) => { if (!controller.signal.aborted) setError(value instanceof Error ? value.message : 'Results could not be loaded.') }); return () => controller.abort() }, [attemptPublicId]); if (error !== null) return <main className="dashboard-page"><p className="form-error">{error}</p></main>; if (result === null) return <main className="dashboard-page"><p>Loading results…</p></main>; return <main className="dashboard-page"><section className="dashboard-card assessment-result"><p className="eyebrow">Attempt {result.attempt.attemptNumber}</p><h1>{result.passed ? 'Assessment passed' : 'Keep building your skills'}</h1><p className="assessment-score">{result.earnedPoints}/{result.totalPoints} · {result.scorePercent}%</p><p>{result.feedback}</p><p>Strongest: <strong>{result.breakdown.strongestTopic.topicTitle}</strong> · Weakest: <strong>{result.breakdown.weakestTopic.topicTitle}</strong></p><div className="topic-breakdown">{result.breakdown.topics.map((topic) => <article key={topic.topicSlug}><strong>{topic.topicTitle}</strong><span>{topic.correctCount}/{topic.totalQuestions} · {topic.status}</span></article>)}</div><div className="assessment-actions"><Link className="button-link" to={`/assessment-attempts/${attemptPublicId}/review`}>Review answers</Link><Link className="button-link button-link--secondary" to={`/assessments/${result.assessment.slug}`}>Assessment history</Link></div></section></main> }
