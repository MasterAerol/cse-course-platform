#!/usr/bin/env node
import { spawnSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'
import { blocksFor, fixedQuestion, generatedByLesson, lessonSpecs, mixedQuestions, quizQuestions, topicDescription, topicSlug, topicTitle, validateQuestions } from './data-interpretation-topic-content.mjs'
import { findUniqueBySlug, parseSuccessEnvelope, requireArray, requireRecord, rollbackStatusChanges } from './logical-reasoning-publisher-helpers.mjs'

export const CONFIRMATION = 'create-validate-publish-data-interpretation'
const csrf = 'same-origin-admin-mutation'
const subjectSlug = 'analytical-ability'
const prerequisiteSlug = 'seating-and-arrangement-problems'
const generatedConfig = (title, slug, status = 'draft') => ({ title, instructions: 'Answer five generated Data Interpretation questions by applying every stated rule in order.', passingScore: 60, questionCount: 5, maximumAttempts: null, showExplanations: true, status, questionSource: 'generated', generatorSlug: slug, generatorVersion: 1, difficulty: { easy: 2, medium: 2, hard: 1 } })
const fixedConfig = (title, status = 'draft') => ({ title, instructions: 'Answer eight fixed questions covering the Data Interpretation skill families.', passingScore: 60, questionCount: 8, maximumAttempts: null, showExplanations: true, status, questionSource: 'fixed' })
const quizConfig = (title, status = 'draft') => ({ title, description: 'A fixed educational topic quiz; these are not official CSC questions.', quizType: 'topic', passingScore: 70, timeLimitMinutes: null, maximumAttempts: null, shuffleQuestions: false, shuffleChoices: false, showExplanations: true, status })

function parseArgs(argv = process.argv) { const args = new Map(); for (let index = 2; index < argv.length; index += 2) { const key = argv[index]; const value = argv[index + 1]; if (key?.startsWith('--') !== true || value === undefined) throw new Error(`Invalid argument near ${key ?? '(end)'}.`); args.set(key.slice(2), value) } return args }
function runGeneratorGate() { const entry = fileURLToPath(new URL('../node_modules/vitest/vitest.mjs', import.meta.url)); const result = spawnSync(process.execPath, [entry, 'run', '--configLoader', 'runner', 'tests/data-interpretation-generators.test.ts'], { stdio: 'inherit', shell: false }); if (result.status !== 0) throw new Error('The 9,000-question Data Interpretation quality gate failed.') }

export async function publishDataInterpretation({ baseUrl, cookie: initialCookie, email, password, skipGeneratorGate = false }) {
  if (!skipGeneratorGate) runGeneratorGate(); let cookie = initialCookie ?? null
  async function request(path, options = {}) { const operation = `${options.method ?? 'GET'} ${path}`; const headers = new Headers(options.headers); headers.set('accept', 'application/json'); if (cookie !== null) headers.set('cookie', cookie); if (options.body !== undefined) headers.set('content-type', 'application/json'); if (options.method !== undefined && options.method !== 'GET') headers.set('x-cse-admin-csrf', csrf); const response = await fetch(`${baseUrl}${path}`, { ...options, headers }); const setCookie = response.headers.get('set-cookie'); if (setCookie !== null) cookie = setCookie.split(';')[0]; const text = await response.text(); let body; try { body = JSON.parse(text) } catch { throw new Error(`${operation} returned non-JSON status ${response.status}.`) } if (!response.ok) { const message = typeof body === 'object' && body !== null && typeof body.error === 'object' && body.error !== null && typeof body.error.message === 'string' ? body.error.message : JSON.stringify(body); throw new Error(`${operation} failed (${response.status}): ${message}`) } return parseSuccessEnvelope(body, operation) }
  if (cookie === null) { if (email === undefined || password === undefined) throw new Error('Pass --cookie, or --email with --password or CSE_DATA_INTERPRETATION_ADMIN_PASSWORD.'); await request('/api/auth/login', { method: 'POST', body: JSON.stringify({ email, password }) }) }
  const dashboard = requireRecord(await request('/api/admin/dashboard'), 'Admin dashboard'); const course = requireRecord(dashboard.cseProfessional, 'CSE Professional course', ['id']); const courseId = course.id
  async function load(label) { const detail = requireRecord(await request(`/api/admin/courses/${courseId}`), `${label} course detail`, ['subjects']); const subject = findUniqueBySlug(requireArray(detail.subjects, `${label} subjects`), subjectSlug, 'Analytical Ability subject'); if (subject === null) throw new Error('Published Analytical Ability subject was not found.'); const record = requireRecord(subject, 'Analytical Ability subject', ['id', 'status', 'topics']); if (record.status !== 'published') throw new Error('Analytical Ability must be published before creating Data Interpretation.'); const topics = requireArray(record.topics, `${label} topics`); const prerequisite = findUniqueBySlug(topics, prerequisiteSlug, 'Seating and Arrangement Problems topic'); if (prerequisite === null || prerequisite.status !== 'published') throw new Error('Published Seating and Arrangement Problems was not found.'); return { subject: record, topics, prerequisite } }
  let curriculum = await load('Initial'); const requiredPosition = curriculum.prerequisite.position + 1; let topic = findUniqueBySlug(curriculum.topics, topicSlug, `${topicTitle} topic`)
  if (topic === null) { const created = requireRecord(await request(`/api/admin/subjects/${curriculum.subject.id}/topics`, { method: 'POST', body: JSON.stringify({ title: topicTitle, slug: topicSlug, description: topicDescription, position: requiredPosition, status: 'draft' }) }), 'Create Data Interpretation response'); topic = requireRecord(created.topic, 'Topic creation returned no record', ['id', 'status']) } else topic = requireRecord(topic, `${topicTitle} topic`, ['id', 'status'])
  if (topic.status !== 'published') { const updated = requireRecord(await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ title: topicTitle, description: topicDescription, position: requiredPosition, status: 'draft', updatedAt: topic.updatedAt }) }), 'Update Data Interpretation response'); topic = requireRecord(updated.topic, 'Topic update returned no record', ['id', 'status']) }
  for (const spec of lessonSpecs) { curriculum = await load(`Creating ${spec.title}`); topic = findUniqueBySlug(curriculum.topics, topicSlug, `${topicTitle} topic`); if (topic === null) throw new Error(`${topicTitle} disappeared during lesson creation.`); const topicRecord = requireRecord(topic, topicTitle, ['id', 'lessons']); const stored = requireArray(topicRecord.lessons, `${topicTitle} lessons`); let lesson = findUniqueBySlug(stored, spec.slug, `${spec.title} lesson`); if (lesson === null) { const created = requireRecord(await request(`/api/admin/topics/${topicRecord.id}/lessons`, { method: 'POST', body: JSON.stringify({ title: spec.title, slug: spec.slug, lessonType: spec.lessonType, summary: `${spec.title} in the Data Interpretation topic.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft' }) }), `Create ${spec.title} response`); lesson = requireRecord(created.lesson, `${spec.title} creation returned no lesson`, ['id', 'status', 'lessonType']) } else lesson = requireRecord(lesson, `${spec.title} lesson`, ['id', 'status', 'lessonType']); if (lesson.lessonType !== spec.lessonType) throw new Error(`${spec.title} has stored type ${lesson.lessonType}; expected ${spec.lessonType}.`); if (lesson.status !== 'published') { const updated = requireRecord(await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ title: spec.title, summary: `${spec.title} in the Data Interpretation topic.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft', updatedAt: lesson.updatedAt }) }), `Update ${spec.title} response`); lesson = requireRecord(updated.lesson, `${spec.title} update returned no lesson`, ['id', 'status']) }
    const blockResponse = requireRecord(await request(`/api/admin/lessons/${lesson.id}/blocks`), `${spec.title} blocks`, ['blocks']); for (const [index, block] of blocksFor(spec.slug).entries()) { const position = index + 1; const existing = requireArray(blockResponse.blocks, 'Lesson blocks').find((item) => item.position === position); await request(existing === undefined ? `/api/admin/lessons/${lesson.id}/blocks` : `/api/admin/lesson-blocks/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify({ ...block, position }) }) }
    const generatorSlug = generatedByLesson[spec.slug]; if (generatorSlug !== undefined) await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(generatedConfig(spec.title, generatorSlug, lesson.status === 'published' ? 'published' : 'draft')) })
    if (spec.slug === 'mixed-data-interpretation-practice') { const saved = requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(fixedConfig(spec.title, lesson.status === 'published' ? 'published' : 'draft')) }), 'Save mixed practice response'); const set = requireRecord(saved.practiceSet, 'Mixed practice save returned no practice set', ['id']); const current = requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`), 'Reload mixed practice response', ['questions']); for (const [index, item] of mixedQuestions.entries()) { const input = fixedQuestion(item, index + 1); const existing = requireArray(current.questions, 'Mixed practice questions').find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/practice-sets/${set.id}/questions` : `/api/admin/practice-questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
    if (spec.slug === 'data-interpretation-topic-quiz') { const saved = requireRecord(await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify(quizConfig(spec.title, lesson.status === 'published' ? 'published' : 'draft')) }), 'Save quiz response'); const quiz = requireRecord(saved.quiz, 'Quiz save returned no quiz', ['id']); const current = requireRecord(await request(`/api/admin/lessons/${lesson.id}/quiz`), 'Reload quiz response', ['questions']); for (const [index, item] of quizQuestions.entries()) { const input = fixedQuestion(item, index + 1, true); const existing = requireArray(current.questions, 'Quiz questions').find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/quizzes/${quiz.id}/questions` : `/api/admin/questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
  }
  const failures = []; const registeredResponse = requireRecord(await request('/api/admin/practice-generators'), 'Practice generator registry', ['generators']); const registered = new Set(requireArray(registeredResponse.generators, 'Registered generators').map((item) => `${item.slug}@${item.version}`)); for (const slug of Object.values(generatedByLesson)) if (!registered.has(`${slug}@1`)) failures.push(`Generator ${slug}@1 is not registered.`); curriculum = await load('Validation'); topic = findUniqueBySlug(curriculum.topics, topicSlug, `${topicTitle} topic`)
  if (topic === null) failures.push(`${topicTitle} is missing.`); else { if (topic.position !== requiredPosition || topic.description !== topicDescription) failures.push('Topic metadata is invalid.'); if (!Array.isArray(topic.lessons) || topic.lessons.length !== 12 || new Set(topic.lessons.map((lesson) => lesson.position)).size !== 12) failures.push('Topic must have exactly 12 uniquely positioned lessons.'); for (const spec of lessonSpecs) { const lesson = topic.lessons?.find((item) => item.slug === spec.slug); if (lesson === undefined) { failures.push(`Missing ${spec.title}.`); continue } if (lesson.lessonType !== spec.lessonType || lesson.requiresPrevious !== true || lesson.isPreview !== false) failures.push(`${spec.title} has invalid type or locking.`); const blocks = requireArray(requireRecord(await request(`/api/admin/lessons/${lesson.id}/blocks`), `${spec.title} blocks`, ['blocks']).blocks, `${spec.title} blocks`); if (blocks.length !== blocksFor(spec.slug).length || blocks.length < 9 || /<\/?[a-z][^>]*>/iu.test(JSON.stringify(blocks))) failures.push(`${spec.title} has invalid teaching blocks.`); const generatorSlug = generatedByLesson[spec.slug]; if (generatorSlug !== undefined) { const set = requireRecord(requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`), `${spec.title} set`).practiceSet, `${spec.title} set`, ['status']); if (set.questionSource !== 'generated' || set.generator?.slug !== generatorSlug || set.generator?.version !== 1 || set.generator?.difficulty.easy !== 2 || set.generator?.difficulty.medium !== 2 || set.generator?.difficulty.hard !== 1 || set.questionCount !== 5 || set.passingScore !== 60 || set.maximumAttempts !== null || set.showExplanations !== true) failures.push(`${spec.title} has invalid generated configuration.`) } if (spec.slug === 'mixed-data-interpretation-practice') failures.push(...validateQuestions('Mixed practice', requireArray(requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`), 'Mixed practice').questions, 'Mixed practice questions'), 8)); if (spec.slug === 'data-interpretation-topic-quiz') failures.push(...validateQuestions('Topic quiz', requireArray(requireRecord(await request(`/api/admin/lessons/${lesson.id}/quiz`), 'Topic quiz').questions, 'Topic quiz questions'), 15)) } }
  if (failures.length > 0) throw new Error(`Validation failed; nothing was published:\n- ${failures.join('\n- ')}`)
  const rollback = []; try { curriculum = await load('Publication'); topic = findUniqueBySlug(curriculum.topics, topicSlug, `${topicTitle} topic`); if (topic === null || !Array.isArray(topic.lessons)) throw new Error(`${topicTitle} disappeared before publication.`); for (const spec of lessonSpecs) { const lesson = topic.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.title} disappeared before publication.`); const generatorSlug = generatedByLesson[spec.slug]; if (generatorSlug !== undefined) { const set = requireRecord(requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`), `${spec.title} set`).practiceSet, `${spec.title} set`, ['status']); if (set.status !== 'published') { const prior = set.status; await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(generatedConfig(spec.title, generatorSlug, 'published')) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(generatedConfig(spec.title, generatorSlug, prior)) })) } } else if (spec.lessonType === 'practice') { const set = requireRecord(requireRecord(await request(`/api/admin/lessons/${lesson.id}/practice-set`), 'Mixed practice').practiceSet, 'Mixed practice set', ['status']); if (set.status !== 'published') { const prior = set.status; await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(fixedConfig(spec.title, 'published')) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify(fixedConfig(spec.title, prior)) })) } } else if (spec.lessonType === 'quiz') { const quiz = requireRecord(requireRecord(await request(`/api/admin/lessons/${lesson.id}/quiz`), 'Topic quiz').quiz, 'Topic quiz', ['status']); if (quiz.status !== 'published') { const prior = quiz.status; await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify(quizConfig(spec.title, 'published')) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify(quizConfig(spec.title, prior)) })) } } }
    curriculum = await load('Lesson publication'); topic = findUniqueBySlug(curriculum.topics, topicSlug, `${topicTitle} topic`); if (topic === null || !Array.isArray(topic.lessons)) throw new Error(`${topicTitle} disappeared before lesson publication.`); for (const spec of lessonSpecs) { const lesson = topic.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.title} disappeared.`); if (lesson.status !== 'published') { const prior = lesson.status; await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: lesson.updatedAt }) }); rollback.push(async () => { const fresh = await load(`Rollback ${spec.title}`); const current = findUniqueBySlug(fresh.topics, topicSlug, topicTitle)?.lessons?.find((item) => item.id === lesson.id); if (current !== undefined) await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: prior, updatedAt: current.updatedAt }) }) }) } }
    curriculum = await load('Topic publication'); topic = findUniqueBySlug(curriculum.topics, topicSlug, topicTitle); if (topic === null) throw new Error(`${topicTitle} disappeared before topic publication.`); if (topic.status !== 'published') { const prior = topic.status; await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: topic.updatedAt }) }); rollback.push(async () => { const fresh = await load('Topic rollback'); const current = findUniqueBySlug(fresh.topics, topicSlug, topicTitle); if (current !== null) await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: prior, updatedAt: current.updatedAt }) }) }) }
  } catch (error) { await rollbackStatusChanges(rollback, (rollbackError) => console.error(`Rollback failed: ${rollbackError instanceof Error ? rollbackError.message : String(rollbackError)}`)); throw error }
  return { courseId, topicSlug, lessons: lessonSpecs.length }
}
async function main() { const args = parseArgs(); if (args.get('confirm') !== CONFIRMATION) throw new Error(`Pass --confirm ${CONFIRMATION} to continue.`); const result = await publishDataInterpretation({ baseUrl: args.get('base-url') ?? 'http://127.0.0.1:5173', cookie: args.get('cookie'), email: args.get('email'), password: args.get('password') ?? process.env.CSE_DATA_INTERPRETATION_ADMIN_PASSWORD }); console.log(`Created, validated, and published ${result.topicSlug} with ${result.lessons} lessons.`) }
if (process.argv[1] !== undefined && fileURLToPath(import.meta.url) === fileURLToPath(new URL(`file:///${process.argv[1].replaceAll('\\', '/')}`))) main().catch((error) => { console.error(error instanceof Error ? error.message : String(error)); process.exitCode = 1 })
