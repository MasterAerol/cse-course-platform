#!/usr/bin/env node

import { spawnSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'

const confirmation = 'create-validate-publish-distance-speed-time'
const csrfHeaderValue = 'same-origin-admin-mutation'
const topicSlug = 'distance-speed-and-time'
const topicDescription = 'A structured course on the relationship among distance, speed, and time, including unit conversions, average speed, relative motion, meeting, and overtaking problems.'

const generatedPracticeByLessonSlug = {
  'finding-distance': 'distance-from-speed-time',
  'finding-speed': 'speed-from-distance-time',
  'finding-time': 'time-from-distance-speed',
  'distance-speed-and-time-unit-conversions': 'travel-unit-conversions',
  'average-speed': 'average-speed',
  'relative-speed-in-the-same-direction': 'same-direction-relative-speed',
  'relative-speed-in-opposite-directions': 'opposite-direction-relative-speed',
  'meeting-and-overtaking-problems': 'meeting-and-overtaking',
  'mixed-travel-problems': 'mixed-distance-speed-time',
}

const lessonSpecs = [
  ['Understanding Distance, Speed, and Time', 'understanding-distance-speed-and-time', 'reading', 12],
  ['Finding Distance', 'finding-distance', 'practice', 10],
  ['Finding Speed', 'finding-speed', 'practice', 10],
  ['Finding Time', 'finding-time', 'practice', 10],
  ['Distance, Speed, and Time Unit Conversions', 'distance-speed-and-time-unit-conversions', 'practice', 13],
  ['Average Speed', 'average-speed', 'practice', 13],
  ['Relative Speed in the Same Direction', 'relative-speed-in-the-same-direction', 'practice', 12],
  ['Relative Speed in Opposite Directions', 'relative-speed-in-opposite-directions', 'practice', 12],
  ['Meeting and Overtaking Problems', 'meeting-and-overtaking-problems', 'practice', 14],
  ['Mixed Travel Problems', 'mixed-travel-problems', 'practice', 15],
  ['Mixed Distance, Speed, and Time Practice', 'mixed-distance-speed-and-time-practice', 'practice', 16],
  ['Distance, Speed, and Time Topic Quiz', 'distance-speed-and-time-topic-quiz', 'quiz', 18],
].map(([title, slug, lessonType, minutes], index) => ({ title, slug, lessonType, minutes, position: index + 1 }))

const heading = (text, level = 2) => ({ blockType: 'heading', content: { level, text } })
const paragraph = (text) => ({ blockType: 'paragraph', content: { text } })
const formula = (expression, description) => ({ blockType: 'formula', content: { expression, description } })
const callout = (title, text, variant = 'info') => ({ blockType: 'callout', content: { title, text, variant } })
const example = (title, problem, steps, answer) => ({ blockType: 'example', content: { title, problem, steps, answer } })
const summary = (items) => ({ blockType: 'summary', content: { items } })

const lessons = {
  'understanding-distance-speed-and-time': ['Distance, speed, and time describe how far an object travels, how quickly it moves, and how long it moves.', 'distance = speed × time', 'Use compatible units before applying a travel formula.', 'A bus travels at 60 km/h for 3 hours.', ['Identify speed = 60 km/h and time = 3 hours.', 'Multiply 60 × 3.'], 'The bus travels 180 kilometers.', 'Do not mix kilometers with meters or hours with minutes.'],
  'finding-distance': ['Distance is the product of a constant speed and elapsed travel time.', 'd = st', 'Convert time to the unit named in the speed before multiplying.', 'A car travels at 70 km/h for 4 hours.', ['Use d = 70 × 4.', 'Keep kilometers as the distance unit.'], 'The distance is 280 kilometers.', 'Do not divide speed by time.'],
  'finding-speed': ['Speed is distance traveled per unit of travel time.', 's = d ÷ t', 'Use travel time, not clock labels, and normalize units first.', 'A train travels 180 km in 3 hours.', ['Use s = 180 ÷ 3.', 'Attach km/h to the quotient.'], 'The speed is 60 km/h.', 'Do not multiply distance and time.'],
  'finding-time': ['Travel time is distance divided by a positive constant speed.', 't = d ÷ s', 'The resulting time unit follows from the distance and speed units.', 'A van covers 240 km at 80 km/h.', ['Use t = 240 ÷ 80.', 'Kilometers cancel, leaving hours.'], 'The trip takes 3 hours.', 'Do not invert the distance-speed relationship.'],
  'distance-speed-and-time-unit-conversions': ['Unit conversion changes representation without changing the physical quantity.', '1 km = 1000 m; 1 h = 60 min = 3600 s; 1 km/h = 5/18 m/s', 'Apply exact conversion factors before using a formula.', 'Convert 72 km/h to m/s.', ['Multiply 72 by 5/18.', 'Reduce before multiplying.'], '72 km/h equals 20 m/s.', 'Do not use 1000/60 for a km/h-to-m/s conversion.'],
  'average-speed': ['Average speed is total distance divided by total elapsed time.', 'average speed = total distance ÷ total time', 'Include every travel leg and any stated stopping time.', 'A traveler covers 60 km at 60 km/h and 60 km at 40 km/h.', ['Times are 1 hour and 1.5 hours.', 'Divide 120 km by 2.5 hours.'], 'Average speed is 48 km/h.', 'Do not take a simple mean of speeds unless travel times are equal.'],
  'relative-speed-in-the-same-direction': ['For same-direction motion, the faster object closes the gap at the difference of the speeds.', 'closing speed = faster speed − slower speed', 'Confirm the faster speed is greater before solving.', 'Vehicles travel at 80 and 60 km/h with a 40 km lead.', ['Closing speed is 20 km/h.', 'Catch time is 40 ÷ 20.'], 'The faster vehicle catches up in 2 hours.', 'Do not add speeds for same-direction pursuit.'],
  'relative-speed-in-opposite-directions': ['Objects moving toward each other close their separation at the sum of their speeds.', 'closing speed = first speed + second speed', 'Use subtraction only for same-direction pursuit.', 'Vehicles 300 km apart approach at 70 and 80 km/h.', ['Closing speed is 150 km/h.', 'Meeting time is 300 ÷ 150.'], 'They meet in 2 hours.', 'Do not subtract speeds when objects approach each other.'],
  'meeting-and-overtaking-problems': ['A delayed departure creates a head start that must be closed after the faster object begins.', 'head start = slower speed × delay; catch time = head start ÷ closing speed', 'State whether the answer is after the second departure or total elapsed time.', 'A 50 km/h bus leaves one hour before a 75 km/h car.', ['The bus has a 50 km head start.', 'The car closes at 25 km/h, requiring 2 hours.'], 'The car catches the bus 2 hours after the car leaves.', 'Do not ignore the delay or add it when only catch time is requested.'],
  'mixed-travel-problems': ['Mixed travel problems combine legs, stops, conversions, and relative motion in a clear timeline.', 'phase distance = phase speed × phase time', 'Normalize units and label each phase before combining totals.', 'A vehicle travels 2 hours at 50 km/h, stops 30 minutes, then travels 3 hours at 70 km/h.', ['Total distance is 310 km.', 'Total elapsed time is 5.5 hours.'], 'Average speed is 56 4/11 km/h.', 'Do not omit stopped time from total elapsed time.'],
}

function lessonBlocks(slug) {
  const data = lessons[slug]
  if (data !== undefined) return [heading(data[0]), paragraph(data[0]), formula(data[1], data[2]), callout('Unit and timeline check', data[2]), example('Worked example', data[3], data[4], data[5]), callout('Common mistake', data[6], 'warning'), heading('Independent practice', 3), summary([data[1], data[2], data[6]])]
  return [heading(slug.includes('quiz') ? 'Topic Quiz Review' : 'Mixed Practice Review'), paragraph('Review direct formulas, exact unit conversions, total-distance average speed, relative motion, delayed starts, and multi-leg travel.'), formula('d = st; s = d/t; t = d/s', 'Choose the formula for the requested quantity.'), callout('Classification', 'Subtract speeds for same-direction pursuit and add speeds for opposite-direction approach.'), example('Review', 'A 50 km/h bus has a one-hour head start over a 75 km/h car.', ['Head start = 50 km.', 'Closing speed = 25 km/h.'], 'Catch time is 2 hours after the car leaves.'), callout('Common mistakes', 'Do not mix units, average speeds naively, omit stop time, or confuse catch time with total elapsed time.', 'warning'), heading('Assessment readiness', 3), summary(['Normalize units first.', 'Use total distance divided by total elapsed time.', 'Label the motion direction and requested clock interval.'])]
}

const mixedQuestions = [
  ['A car travels at 70 km/h for 4 hours. How far does it travel?', ['280 kilometers', '17.5 kilometers', '74 kilometers', '350 kilometers'], 0, 'Distance = 70 × 4 = 280 kilometers.'],
  ['A train covers 180 kilometers in 3 hours. What is its speed?', ['60 km/h', '540 km/h', '177 km/h', '90 km/h'], 0, 'Speed = 180 ÷ 3 = 60 km/h.'],
  ['A van covers 240 kilometers at 80 km/h. How long does it take?', ['3 hours', '19,200 hours', '160 hours', '4 hours'], 0, 'Time = 240 ÷ 80 = 3 hours.'],
  ['Convert 72 km/h to m/s.', ['20 m/s', '12.96 m/s', '259.2 m/s', '40 m/s'], 0, 'Multiply 72 by 5/18 to get 20 m/s.'],
  ['A traveler covers 60 km at 60 km/h and 60 km at 40 km/h. What is the average speed?', ['48 km/h', '50 km/h', '100 km/h', '40 km/h'], 0, 'Total distance 120 divided by total time 2.5 gives 48 km/h.'],
  ['Vehicles at 80 and 60 km/h move in the same direction. What is their closing speed?', ['20 km/h', '140 km/h', '40 km/h', '70 km/h'], 0, 'Same-direction closing speed is 80 − 60 = 20 km/h.'],
  ['Vehicles at 70 and 80 km/h move toward each other. What is their closing speed?', ['150 km/h', '10 km/h', '75 km/h', '5600 km/h'], 0, 'Opposite-direction closing speed is 70 + 80 = 150 km/h.'],
  ['A 50 km/h bus leaves one hour before a 75 km/h car. How long after the car leaves does it catch the bus?', ['2 hours', '1 hour', '3 hours', '5 hours'], 0, 'The 50 km head start closes at 25 km/h, so catch time is 2 hours.'],
]

const quizQuestions = [
  ['Which equation finds distance?', ['distance = speed × time', 'distance = speed ÷ time', 'distance = time ÷ speed', 'distance = speed + time'], 0, 'Distance equals speed multiplied by compatible elapsed time.'],
  ['A vehicle travels at 60 km/h for 3 hours. How far?', ['180 kilometers', '20 kilometers', '63 kilometers', '120 kilometers'], 0, '60 × 3 = 180 kilometers.'],
  ['A vehicle covers 150 km in 2.5 hours. What is its speed?', ['60 km/h', '375 km/h', '147.5 km/h', '75 km/h'], 0, '150 ÷ 2.5 = 60 km/h.'],
  ['A vehicle covers 240 km at 80 km/h. How long?', ['3 hours', '160 hours', '320 hours', '4 hours'], 0, '240 ÷ 80 = 3 hours.'],
  ['How many hours is 2 hours 30 minutes?', ['2.5 hours', '2.3 hours', '2.03 hours', '150 hours'], 0, '30 minutes is 0.5 hour, giving 2.5 hours.'],
  ['Convert 72 km/h to m/s.', ['20 m/s', '40 m/s', '12.96 m/s', '259.2 m/s'], 0, '72 × 5/18 = 20 m/s.'],
  ['Convert 15 m/s to km/h.', ['54 km/h', '27 km/h', '75 km/h', '270 km/h'], 0, '15 × 18/5 = 54 km/h.'],
  ['A vehicle spends 2 hours at 50 km/h and 2 hours at 70 km/h. Average speed?', ['60 km/h', '120 km/h', '50 km/h', '70 km/h'], 0, 'Equal times give 240 km over 4 hours = 60 km/h.'],
  ['A traveler covers 60 km at 60 km/h and 60 km at 40 km/h. Average speed?', ['48 km/h', '50 km/h', '100 km/h', '40 km/h'], 0, '120 km over 2.5 hours = 48 km/h.'],
  ['A trip covers 150 km in 2 hours, stops 30 minutes, then 100 km in 1.5 hours. Average speed?', ['62.5 km/h', '71 3/7 km/h', '100 km/h', '50 km/h'], 0, '250 km divided by 4 total elapsed hours is 62.5 km/h.'],
  ['Same-direction speeds are 80 and 60 km/h. Closing speed?', ['20 km/h', '140 km/h', '70 km/h', '40 km/h'], 0, 'Subtract for same-direction motion: 80 − 60 = 20 km/h.'],
  ['Opposite-direction speeds are 70 and 80 km/h. Closing speed?', ['150 km/h', '10 km/h', '75 km/h', '5600 km/h'], 0, 'Add for opposite-direction motion: 70 + 80 = 150 km/h.'],
  ['A 50 km/h bus leaves at 8:00 and a 75 km/h car at 9:00. When does the car catch it?', ['11:00', '10:00', '12:00', '1:00'], 0, 'The car needs 2 hours after 9:00, so it catches the bus at 11:00.'],
  ['Two vehicles 420 km apart approach at 60 and 80 km/h. How far does the first travel before meeting?', ['180 kilometers', '240 kilometers', '3 kilometers', '140 kilometers'], 0, 'They meet after 420/140 = 3 hours; the first travels 60 × 3 = 180 km.'],
  ['A vehicle travels 2 hours at 50 km/h and 3 hours at 70 km/h without stopping. Average speed?', ['62 km/h', '60 km/h', '120 km/h', '310 km/h'], 0, 'Total distance is 310 km and time is 5 hours, so average speed is 62 km/h.'],
]

function parseArgs() { const args = new Map(); for (let index = 2; index < process.argv.length; index += 2) { const key = process.argv[index]; const value = process.argv[index + 1]; if (key?.startsWith('--') !== true || value === undefined) throw new Error(`Invalid argument near ${key ?? '(end)'}.`); args.set(key.slice(2), value) } return args }
function fixedQuestion(item, position, quiz = false) { const [prompt, choices, correctIndex, explanation] = item; return { ...(quiz ? { questionType: 'multiple_choice' } : {}), prompt, explanation, points: 1, position, status: 'active', choices: choices.map((text, index) => ({ text, isCorrect: index === correctIndex, position: index + 1 })) } }
function validateQuestions(label, questions, expected) { const failures = []; if (questions.length !== expected) failures.push(`${label} must have ${expected} questions; found ${questions.length}.`); for (const question of questions) { if (question.choices.length !== 4) failures.push(`${label} question ${question.position} does not have four choices.`); if (question.choices.filter((choice) => choice.isCorrect).length !== 1) failures.push(`${label} question ${question.position} does not have one correct choice.`); if (new Set(question.choices.map((choice) => choice.text.trim())).size !== 4) failures.push(`${label} question ${question.position} has duplicate choices.`); if ((question.explanation ?? '').trim().length < 15) failures.push(`${label} question ${question.position} lacks an explanation.`) } return failures }
const hasRawHtml = (value) => /<\/?[a-z][^>]*>/iu.test(JSON.stringify(value))
function runGeneratorQualityGate() { const entry = fileURLToPath(new URL('../node_modules/vitest/vitest.mjs', import.meta.url)); const result = spawnSync(process.execPath, [entry, 'run', 'tests/worker.test.ts', '-t', 'Dynamic distance-speed-time generator engine'], { stdio: 'inherit', shell: false }); if (result.status !== 0) throw new Error('The 9,000-question distance-speed-time quality gate failed.') }

async function main() {
  const args = parseArgs(); const baseUrl = args.get('base-url') ?? 'http://127.0.0.1:5173'; if (args.get('confirm') !== confirmation) throw new Error(`Pass --confirm ${confirmation} to continue.`); runGeneratorQualityGate(); let cookie = args.get('cookie') ?? null
  async function request(path, options = {}) { const headers = new Headers(options.headers); headers.set('accept', 'application/json'); if (cookie !== null) headers.set('cookie', cookie); if (options.body !== undefined) headers.set('content-type', 'application/json'); if (options.method !== undefined && options.method !== 'GET') headers.set('x-cse-admin-csrf', csrfHeaderValue); const response = await fetch(`${baseUrl}${path}`, { ...options, headers }); const setCookie = response.headers.get('set-cookie'); if (setCookie !== null) cookie = setCookie.split(';')[0]; const body = await response.json(); if (!response.ok || body.success !== true) throw new Error(`${options.method ?? 'GET'} ${path} failed: ${JSON.stringify(body)}`); return body.data }
  if (cookie === null) { const email = args.get('email'); const password = args.get('password') ?? process.env.CSE_DISTANCE_SPEED_TIME_ADMIN_PASSWORD; if (email === undefined || password === undefined) throw new Error('Pass --cookie, or --email with --password or CSE_DISTANCE_SPEED_TIME_ADMIN_PASSWORD.'); await request('/api/auth/login', { method: 'POST', body: JSON.stringify({ email, password }) }) }
  const dashboard = await request('/api/admin/dashboard'); const courseId = dashboard.cseProfessional?.id; if (courseId === undefined) throw new Error('CSE Professional course was not found.')
  let detail = await request(`/api/admin/courses/${courseId}`); let subject = detail.subjects.find((item) => item.slug === 'numerical-ability'); if (subject === undefined) throw new Error('Numerical Ability subject was not found.'); let previousTopic = subject.topics.find((item) => item.slug === 'work-and-rate-problems'); if (previousTopic === undefined || previousTopic.status !== 'published') throw new Error('Published Work and Rate Problems must exist before Distance, Speed, and Time.')
  let topic = subject.topics.find((item) => item.slug === topicSlug); if (topic === undefined) topic = (await request(`/api/admin/subjects/${subject.id}/topics`, { method: 'POST', body: JSON.stringify({ title: 'Distance, Speed, and Time', slug: topicSlug, description: topicDescription, position: previousTopic.position + 1, status: 'draft' }) })).topic; else if (topic.status !== 'published') topic = (await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ title: 'Distance, Speed, and Time', description: topicDescription, position: previousTopic.position + 1, status: 'draft', updatedAt: topic.updatedAt }) })).topic
  for (const spec of lessonSpecs) {
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug); if (topic === undefined) throw new Error('Distance, Speed, and Time disappeared during creation.')
    let lesson = topic.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) lesson = (await request(`/api/admin/topics/${topic.id}/lessons`, { method: 'POST', body: JSON.stringify({ title: spec.title, slug: spec.slug, lessonType: spec.lessonType, summary: `${spec.title} in the Distance, Speed, and Time topic.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft' }) })).lesson; else if (lesson.lessonType !== spec.lessonType) throw new Error(`${spec.slug} has stored type ${lesson.lessonType}; expected ${spec.lessonType}.`); else if (lesson.status !== 'published') lesson = (await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ title: spec.title, summary: `${spec.title} in the Distance, Speed, and Time topic.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft', updatedAt: lesson.updatedAt }) })).lesson
    const currentBlocks = await request(`/api/admin/lessons/${lesson.id}/blocks`); for (const [index, block] of lessonBlocks(spec.slug).entries()) { const position = index + 1; const existing = currentBlocks.blocks.find((item) => item.position === position); await request(existing === undefined ? `/api/admin/lessons/${lesson.id}/blocks` : `/api/admin/lesson-blocks/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify({ ...block, position }) }) }
    const generatorSlug = generatedPracticeByLessonSlug[spec.slug]; if (generatorSlug !== undefined) await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ title: spec.title, instructions: 'Answer five generated travel questions using exact units and review the explanations.', passingScore: 60, questionCount: 5, maximumAttempts: null, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft', questionSource: 'generated', generatorSlug, generatorVersion: 1, difficulty: { easy: 2, medium: 2, hard: 1 } }) })
    if (spec.slug === 'mixed-distance-speed-and-time-practice') { const saved = await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ title: spec.title, instructions: 'Solve eight fixed distance, speed, time, conversion, average-speed, and relative-motion questions.', passingScore: 60, questionCount: 8, maximumAttempts: null, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft', questionSource: 'fixed' }) }); const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); for (const [index, item] of mixedQuestions.entries()) { const input = fixedQuestion(item, index + 1); const existing = current.questions.find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/practice-sets/${saved.practiceSet.id}/questions` : `/api/admin/practice-questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
    if (spec.slug === 'distance-speed-and-time-topic-quiz') { const saved = await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ title: spec.title, description: 'A fixed 15-question Distance, Speed, and Time topic quiz.', quizType: 'topic', passingScore: 70, timeLimitMinutes: null, maximumAttempts: null, shuffleQuestions: false, shuffleChoices: false, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft' }) }); const current = await request(`/api/admin/lessons/${lesson.id}/quiz`); for (const [index, item] of quizQuestions.entries()) { const input = fixedQuestion(item, index + 1, true); const existing = current.questions.find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/quizzes/${saved.quiz.id}/questions` : `/api/admin/questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
  }
  const failures = []; const registry = await request('/api/admin/practice-generators'); const registered = new Set(registry.generators.map((item) => `${item.slug}@${item.version}`)); for (const slug of Object.values(generatedPracticeByLessonSlug)) if (!registered.has(`${slug}@1`)) failures.push(`Generator ${slug}@1 is not registered.`)
  detail = await request(`/api/admin/courses/${courseId}`); subject = detail.subjects.find((item) => item.slug === 'numerical-ability'); previousTopic = subject?.topics.find((item) => item.slug === 'work-and-rate-problems'); topic = subject?.topics.find((item) => item.slug === topicSlug)
  if (topic === undefined) failures.push('Distance, Speed, and Time was not found.'); else { if (topic.description !== topicDescription) failures.push('Topic description is incorrect.'); if (previousTopic === undefined || previousTopic.status !== 'published' || topic.position !== previousTopic.position + 1) failures.push('Topic is not immediately after published Work and Rate Problems.'); if (topic.lessons.length !== 12 || new Set(topic.lessons.map((item) => item.position)).size !== 12) failures.push('The topic must have 12 uniquely positioned lessons.'); for (const spec of lessonSpecs) { const lesson = topic.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) { failures.push(`Missing ${spec.slug}.`); continue } if (lesson.lessonType !== spec.lessonType || lesson.requiresPrevious !== true || lesson.isPreview !== false) failures.push(`${spec.slug} has invalid type or locking.`); const blocks = await request(`/api/admin/lessons/${lesson.id}/blocks`); if (blocks.blocks.length < 8 || blocks.blocks.some((block) => hasRawHtml(block.content))) failures.push(`${spec.slug} has invalid teaching blocks.`); const slug = generatedPracticeByLessonSlug[spec.slug]; if (slug !== undefined) { const set = (await request(`/api/admin/lessons/${lesson.id}/practice-set`)).practiceSet; if (set?.questionSource !== 'generated' || set.generator?.slug !== slug || set.generator?.version !== 1 || set.generator?.difficulty.easy !== 2 || set.generator?.difficulty.medium !== 2 || set.generator?.difficulty.hard !== 1 || set.questionCount !== 5 || set.passingScore !== 60 || set.maximumAttempts !== null || set.showExplanations !== true) failures.push(`${spec.slug} has invalid generated configuration.`) } if (spec.slug === 'mixed-distance-speed-and-time-practice') failures.push(...validateQuestions('Mixed practice', (await request(`/api/admin/lessons/${lesson.id}/practice-set`)).questions, 8)); if (spec.slug === 'distance-speed-and-time-topic-quiz') failures.push(...validateQuestions('Topic quiz', (await request(`/api/admin/lessons/${lesson.id}/quiz`)).questions, 15)) } }
  if (failures.length > 0) { console.error('Distance, Speed, and Time validation failed. Nothing was published.'); for (const failure of failures) console.error(`- ${failure}`); process.exitCode = 1; return }
  const rollback = []
  try { detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug); for (const spec of lessonSpecs) { const lesson = topic?.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.slug} disappeared before publication.`); const slug = generatedPracticeByLessonSlug[spec.slug]; if (slug !== undefined) { const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); const config = { title: spec.title, instructions: 'Answer five generated travel questions using exact units and review the explanations.', passingScore: 60, questionCount: 5, maximumAttempts: null, showExplanations: true, questionSource: 'generated', generatorSlug: slug, generatorVersion: 1, difficulty: { easy: 2, medium: 2, hard: 1 } }; if (current.practiceSet.status !== 'published') { await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.practiceSet.status }) })) } } if (spec.slug === 'mixed-distance-speed-and-time-practice') { const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); const config = { title: spec.title, instructions: 'Solve eight fixed distance, speed, time, conversion, average-speed, and relative-motion questions.', passingScore: 60, questionCount: 8, maximumAttempts: null, showExplanations: true, questionSource: 'fixed' }; if (current.practiceSet.status !== 'published') { await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.practiceSet.status }) })) } } if (spec.slug === 'distance-speed-and-time-topic-quiz') { const current = await request(`/api/admin/lessons/${lesson.id}/quiz`); const config = { title: spec.title, description: 'A fixed 15-question Distance, Speed, and Time topic quiz.', quizType: 'topic', passingScore: 70, timeLimitMinutes: null, maximumAttempts: null, shuffleQuestions: false, shuffleChoices: false, showExplanations: true }; if (current.quiz.status !== 'published') { await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.quiz.status }) })) } } }
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug); for (const spec of lessonSpecs) { const lesson = topic?.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.slug} disappeared.`); if (lesson.status !== 'published') { await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: lesson.updatedAt }) }); rollback.push(async () => { const refreshed = await request(`/api/admin/courses/${courseId}`); const current = refreshed.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug)?.lessons.find((item) => item.id === lesson.id); if (current !== undefined) return request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: lesson.status, updatedAt: current.updatedAt }) }) }) } }
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug); if (topic === undefined) throw new Error('Topic disappeared.'); if (topic.status !== 'published') { const prior = topic.status; await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: topic.updatedAt }) }); rollback.push(async () => { const refreshed = await request(`/api/admin/courses/${courseId}`); const current = refreshed.subjects.find((item) => item.slug === 'numerical-ability')?.topics.find((item) => item.slug === topicSlug); if (current !== undefined) return request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: prior, updatedAt: current.updatedAt }) }) }) }
  } catch (error) { console.error(`Publication failed: ${error instanceof Error ? error.message : String(error)}`); console.error('Rolling back statuses changed during this run.'); for (const action of rollback.reverse()) try { await action() } catch (rollbackError) { console.error(`Rollback failed: ${rollbackError instanceof Error ? rollbackError.message : String(rollbackError)}`) } process.exitCode = 1; return }
  console.log('Distance, Speed, and Time was created, validated, and published.'); for (const spec of lessonSpecs) console.log(`- ${spec.position}. ${spec.title} (${spec.lessonType})`)
}

main().catch((error) => { console.error(error); process.exitCode = 1 })
