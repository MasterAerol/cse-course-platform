#!/usr/bin/env node

import { spawnSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'
import { findUniqueBySlug, planAnalyticalSubject, rollbackStatusChanges } from './logical-reasoning-publisher-helpers.mjs'

export const CONFIRMATION = 'create-validate-publish-logical-reasoning-fundamentals'
const csrf = 'same-origin-admin-mutation'
const subjectSlug = 'analytical-ability'
const topicSlug = 'logical-reasoning-fundamentals'
const subjectDescription = 'Training in logical reasoning, pattern recognition, deductions, relationships, and analytical problem solving.'
const topicDescription = 'A structured introduction to statements, conclusions, assumptions, deductions, conditions, and basic logical relationships.'

const generatedByLesson = {
  'facts-opinions-and-conclusions': 'fact-opinion-conclusion',
  'identifying-valid-conclusions': 'valid-conclusion',
  'assumptions-and-hidden-premises': 'assumption-identification',
  'if-then-statements': 'conditional-reasoning',
  'necessary-and-sufficient-conditions': 'necessary-sufficient-condition',
  'negation-and-contradiction': 'negation-contradiction',
  'basic-deductive-reasoning': 'basic-deduction',
  'logical-equivalence': 'logical-equivalence',
  'mixed-logical-reasoning-problems': 'mixed-logical-reasoning',
}

export const lessonSpecs = [
  ['Understanding Logical Statements', 'understanding-logical-statements', 'reading', 12],
  ['Facts, Opinions, and Conclusions', 'facts-opinions-and-conclusions', 'practice', 12],
  ['Identifying Valid Conclusions', 'identifying-valid-conclusions', 'practice', 14],
  ['Assumptions and Hidden Premises', 'assumptions-and-hidden-premises', 'practice', 14],
  ['If–Then Statements', 'if-then-statements', 'practice', 14],
  ['Necessary and Sufficient Conditions', 'necessary-and-sufficient-conditions', 'practice', 15],
  ['Negation and Contradiction', 'negation-and-contradiction', 'practice', 15],
  ['Basic Deductive Reasoning', 'basic-deductive-reasoning', 'practice', 16],
  ['Logical Equivalence', 'logical-equivalence', 'practice', 15],
  ['Mixed Logical Reasoning Problems', 'mixed-logical-reasoning-problems', 'practice', 18],
  ['Mixed Logical Reasoning Practice', 'mixed-logical-reasoning-practice', 'practice', 20],
  ['Logical Reasoning Fundamentals Topic Quiz', 'logical-reasoning-fundamentals-topic-quiz', 'quiz', 25],
].map(([title, slug, lessonType, minutes], index) => ({ title, slug, lessonType, minutes, position: index + 1 }))

const heading = (text, level = 2) => ({ blockType: 'heading', content: { level, text } })
const paragraph = (text) => ({ blockType: 'paragraph', content: { text } })
const callout = (title, text, variant = 'info') => ({ blockType: 'callout', content: { title, text, variant } })
const example = (title, problem, steps, answer) => ({ blockType: 'example', content: { title, problem, steps, answer } })
const summary = (items) => ({ blockType: 'summary', content: { items } })

const teaching = {
  'facts-opinions-and-conclusions': ['A stated fact is information explicitly supplied as true for the problem. An opinion expresses a judgment. A conclusion must follow from the supplied facts.', 'Treat the passage as a closed world: use its statements, not real-world beliefs.', ['Fact and opinion', 'A record says five forms arrived. A clerk says the filing system is excellent.', ['The record count is supplied evidence.', '“Excellent” is a judgment.'], 'The count is the stated fact.'], ['Supported conclusion', 'All approved forms are signed. Form K is approved.', ['Apply the stated rule to Form K.', 'Do not add facts not present.'], 'Form K is signed.'], 'Do not confuse praise with evidence, copy a premise when an inference is requested, or overgeneralize.'],
  'identifying-valid-conclusions': ['A valid conclusion must be true whenever all stated premises are true.', 'Trace category membership or a rule one step at a time; reverse no arrows.', ['Category chain', 'All team leads attend the briefing. Mara is a team lead.', ['Place Mara in the team-lead group.', 'Apply the attendance rule.'], 'Mara attends the briefing.'], ['Contrapositive', 'Every approved form is signed. Form X is not signed.', ['Approved implies signed.', 'Not signed implies not approved.'], 'Form X is not approved.'], 'Avoid affirming the consequent, denying the antecedent, reversing a universal statement, and claiming more than the premises support.'],
  'assumptions-and-hidden-premises': ['An assumption is an unstated idea an argument needs in order for its reason to support its conclusion.', 'Choose the narrow bridge between the evidence and conclusion, not a stronger opinion.', ['Printer argument', 'Add a printer because employees wait to print.', ['The reason concerns waiting.', 'The needed bridge is that printer availability contributes to the delay.'], 'Availability must contribute to the wait.'], ['Schedule argument', 'Move a meeting earlier so Ana can attend.', ['The proposal changes time.', 'It assumes the earlier time permits Ana to attend.'], 'Ana must be available at the earlier time.'], 'Do not select the conclusion itself, irrelevant background, or an unnecessarily strong claim.'],
  'if-then-statements': ['A conditional rule “If P, then Q” makes P sufficient for Q.', 'Valid forms are P, therefore Q; and not Q, therefore not P.', ['Modus ponens', 'If a form is approved, it is logged. Form A is approved.', ['Use P → Q.', 'P holds for Form A.'], 'Form A is logged.'], ['Modus tollens', 'If a task is complete, it is signed. Task B is not signed.', ['Use not Q → not P.', 'The missing signature denies Q.'], 'Task B is not complete.'], 'Do not infer P from Q or infer not Q from not P. Those are affirming the consequent and denying the antecedent.'],
  'necessary-and-sufficient-conditions': ['A sufficient condition guarantees a result. A necessary condition is required but may not guarantee the result.', '“If” introduces a sufficient condition; “only if” introduces a necessary condition.', ['Sufficient', 'If a pass is gold, it grants gate access.', ['Gold is the stated trigger.', 'Within the rule, gold guarantees access.'], 'Gold is sufficient for access.'], ['Necessary', 'A worker enters only if an ID is shown.', ['“Only if” marks a requirement.', 'Other requirements may still apply.'], 'Showing an ID is necessary, not automatically sufficient.'], 'Do not reverse the condition or treat every requirement as a guarantee.'],
  'negation-and-contradiction': ['A negation is true exactly when the original statement is false.', 'Negate “all” with at least one counterexample; negate “some” with none.', ['Universal negation', 'Negate: All clerks are trained.', ['One untrained clerk is enough to make “all” false.', 'Keep the same subject group.'], 'At least one clerk is not trained.'], ['Conjunction', 'Negate: The office is open and the supervisor is present.', ['The conjunction fails if either part fails.', 'Use inclusive “or.”'], 'The office is not open or the supervisor is not present.'], 'Do not replace all with none, negate only a convenient part, or confuse a contrary with an exact contradiction.'],
  'basic-deductive-reasoning': ['Deduction combines stated premises so the conclusion is guaranteed.', 'Write a chain and apply each link in its stated direction.', ['Two steps', 'All leaders brief. Everyone who briefs signs. Mara leads.', ['Mara briefs.', 'Everyone who briefs signs.'], 'Mara signs.'], ['Elimination', 'Lio works Monday or Tuesday. Lio does not work Monday.', ['One listed option must hold.', 'Monday is eliminated.'], 'Lio works Tuesday.'], 'Do not choose what merely may be true, skip a link, reverse a relation, or import an unstated fact.'],
  'logical-equivalence': ['Equivalent statements have the same truth conditions.', 'A conditional equals its contrapositive; and/or order may be swapped; double negation restores the statement.', ['Contrapositive', 'If approved, then signed.', ['Let P be approved and Q be signed.', 'Use not Q → not P.'], 'If not signed, then not approved.'], ['Not both', 'It is not true that both P and Q hold.', ['Negate the conjunction.', 'Apply De Morgan’s rule.'], 'Not P or not Q.'], 'The converse and inverse are not equivalent to a conditional, and “not both” does not mean “neither.”'],
  'mixed-logical-reasoning-problems': ['Mixed problems first require identifying the logical structure being tested.', 'Label premises, conditions, quantifiers, and the requested conclusion before evaluating options.', ['Mixed conditional', 'If filed, logged. Record R is not logged.', ['Recognize modus tollens.', 'Use not logged → not filed.'], 'Record R is not filed.'], ['Mixed chain', 'All A are B; all B are C; Nia is A.', ['Follow A → B.', 'Then follow B → C.'], 'Nia is C.'], 'Do not switch rules midway, reverse an arrow, strengthen a quantifier, or use outside knowledge.'],
}

export function blocksFor(slug) {
  if (slug === 'understanding-logical-statements') return [
    heading('Logical statements make truth-valued claims'), paragraph('A logical statement is a declarative sentence that can be true or false. Logical reasoning evaluates the structure of the claim, not whether a reader personally believes it.'),
    heading('Statements and non-statements', 3), callout('Truth-language guide', 'Ask whether the sentence asserts one or more claims. Questions seek information; commands request action; exclamations express a reaction and generally are not propositions.'),
    example('Statement', '“All clerks submitted the report.”', ['It asserts a definite claim.', 'The claim can be evaluated as true or false.'], 'This is a logical statement.'),
    example('Command', '“Submit the report.”', ['It requests an action.', 'It does not assert a truth-valued claim.'], 'This is a command, not a proposition.'),
    example('Question', '“Did the meeting begin?”', ['It asks for information.', 'It does not itself assert an answer.'], 'This is a question, not a proposition.'),
    heading('Simple and compound statements', 3), example('Compound statement', '“The office is open and the supervisor is present.”', ['The sentence asserts that the office is open.', 'It also asserts that the supervisor is present.'], 'Two claims joined by “and” form a compound statement.'),
    callout('Use only the given information', 'A conclusion must follow from the supplied premises. Do not repair a problem with assumptions about offices, people, or ordinary life.', 'important'),
    callout('Common mistakes', 'Do not treat every complete sentence as a proposition, classify a question or command as a statement, or confuse a long sentence with a compound logical claim.', 'warning'),
    summary(['Statements can be true or false.', 'Questions, commands, and exclamations generally are not statements.', 'Compound statements combine claims.', 'Judge structure using only the given information.']),
  ]
  const item = teaching[slug]
  if (item !== undefined) return [heading(item[0]), paragraph(item[0]), callout('Reasoning rule', item[1]), example(...item[2]), example(...item[3]), callout('Common mistakes', item[4], 'warning'), heading('Practice method', 3), paragraph('Read the prompt as a complete rule system, identify the exact relation, and eliminate each option by naming the rule it violates.'), summary([item[1], item[4], 'Choose only what must follow from the supplied information.'])]
  return [heading('Assessment review'), paragraph('Review statements, evidence, conclusions, assumptions, conditional direction, quantifier negation, deduction, and equivalence before starting.'), callout('Closed-world rule', 'Use only the facts and rules in each question.'), example('Conditional review', 'If approved, then logged. A is approved.', ['Match the stated antecedent.', 'Apply modus ponens.'], 'A is logged.'), example('Negation review', 'Negate: All forms are signed.', ['Look for a counterexample.', 'Change all to at least one not.'], 'At least one form is not signed.'), callout('Common mistakes', 'Avoid converse errors, overgeneralization, outside assumptions, partial negation, and stronger-than-needed assumptions.', 'warning'), heading('Ready check', 3), paragraph('For each answer, be able to name the premise and rule that guarantees it.'), summary(['Follow arrows only in valid directions.', 'Negate quantifiers exactly.', 'Prefer necessary conclusions over possible claims.'])]
}

export const mixedQuestions = [
  ['Which sentence is a logical statement?', ['The office opens at eight.', 'Open the office.', 'Does the office open at eight?', 'What a busy office!'], 0, 'A declarative claim can be evaluated as true or false.'],
  ['A log states that six forms arrived. Which is the stated fact?', ['Six forms arrived.', 'The forms arrived too late.', 'Six is the ideal number of forms.', 'The clerk likes the forms.'], 0, 'The count is explicitly supplied by the log.'],
  ['All leaders attend the briefing. Mara is a leader. What follows?', ['Mara attends the briefing.', 'Everyone at the briefing is a leader.', 'Mara leads every team.', 'No leader misses any meeting.'], 0, 'Apply the universal rule directly to Mara.'],
  ['“Add a printer because employees wait to print.” What assumption is required?', ['Printer availability contributes to the wait.', 'Every employee owns a printer.', 'Printing is the most important office task.', 'The office will add a printer.'], 0, 'The reason supports the proposal only if availability contributes to the delay.'],
  ['If a form is approved, it is logged. Form A is approved. What follows?', ['Form A is logged.', 'Every logged form is approved.', 'Form A is not logged.', 'Form A was submitted today.'], 0, 'Modus ponens applies P → Q to P.'],
  ['A worker enters only if an ID is shown. Showing an ID is what kind of condition for entry?', ['Necessary', 'Automatically sufficient', 'Irrelevant', 'A contradiction'], 0, '“Only if” introduces a necessary condition.'],
  ['What is the negation of “All clerks are trained”?', ['At least one clerk is not trained.', 'No clerk is trained.', 'At least one clerk is trained.', 'All trained people are clerks.'], 0, 'A single counterexample negates a universal statement.'],
  ['All leaders brief. Everyone who briefs signs. Mara leads. What follows?', ['Mara signs.', 'Everyone who signs leads.', 'Mara signs only if nobody briefs.', 'All employees lead.'], 0, 'Follow the two-step chain: leads → briefs → signs.'],
]

export const quizQuestions = [
  ['Which is a logical statement?', ['The record is complete.', 'Complete the record.', 'Is the record complete?', 'What a complete record!'], 0, 'The declarative sentence asserts a truth-valued claim.'],
  ['Which is a compound statement?', ['The office is open and the supervisor is present.', 'Open the office.', 'Is the office open?', 'The supervisor'], 0, 'The first option combines two claims with “and.”'],
  ['A schedule states the meeting begins at nine. Which is the stated fact?', ['The meeting begins at nine.', 'Nine is the best meeting time.', 'The meeting will be productive.', 'Everyone prefers nine.'], 0, 'The schedule explicitly supplies the starting time.'],
  ['All approved forms are signed. Form K is approved. What is supported?', ['Form K is signed.', 'Every signed form is approved.', 'Form K is the only approved form.', 'No other form is signed.'], 0, 'Apply approved → signed to Form K.'],
  ['All approved forms are signed. Which conclusion is invalid?', ['Every signed form is approved.', 'An approved form is signed.', 'A form without a signature is not approved.', 'If K is approved, K is signed.'], 0, 'Reversing approved → signed is not valid.'],
  ['“Move the meeting earlier so Ana can attend.” What assumption is required?', ['Ana can attend at the earlier time.', 'Earlier meetings are always better.', 'Everyone wants Ana present.', 'The meeting has already moved.'], 0, 'The proposed time change must enable the claimed result.'],
  ['If filed, then logged. Record R is filed. What follows?', ['R is logged.', 'If logged, then filed.', 'R is not filed.', 'Every record is logged.'], 0, 'This is modus ponens.'],
  ['If complete, then signed. Task T is not signed. What follows?', ['T is not complete.', 'T is complete.', 'Every signed task is complete.', 'T was started late.'], 0, 'Modus tollens uses not-Q → not-P.'],
  ['If approved, then logged. A is logged, so A is approved. What error occurs?', ['Affirming the consequent', 'Modus ponens', 'Modus tollens', 'Valid contraposition'], 0, 'The argument invalidly infers P from Q.'],
  ['A worker enters only if an ID is shown. ID presentation is what for entry?', ['Necessary', 'Sufficient by itself', 'Unrelated', 'Equivalent to being employed'], 0, '“Only if” marks a necessary condition.'],
  ['If a pass is gold, it grants access. Being gold is what for access under this rule?', ['Sufficient', 'Necessary for every access case', 'A contradiction', 'An opinion'], 0, 'The antecedent guarantees the consequent.'],
  ['Negate “All reports are filed.”', ['At least one report is not filed.', 'No report is filed.', 'Some report is filed.', 'All files are reports.'], 0, 'Negating all requires at least one counterexample.'],
  ['Which directly contradicts “Mara is present”?', ['Mara is not present.', 'Mara may be present.', 'Lio is present.', 'Mara arrived early.'], 0, 'P and not-P are direct contradictions.'],
  ['Which is equivalent to “If approved, then signed”?', ['If not signed, then not approved.', 'If signed, then approved.', 'If not approved, then not signed.', 'If approved, then not signed.'], 0, 'A conditional is equivalent to its contrapositive.'],
  ['All leaders brief. Everyone who briefs signs. Mara leads. What must be true?', ['Mara signs.', 'Everyone who signs leads.', 'Only Mara briefs.', 'All employees sign.'], 0, 'The chain leads → briefs → signs guarantees that Mara signs.'],
]

function parseArgs(argv = process.argv) { const args = new Map(); for (let i = 2; i < argv.length; i += 2) { const key = argv[i]; const value = argv[i + 1]; if (key?.startsWith('--') !== true || value === undefined) throw new Error(`Invalid argument near ${key ?? '(end)'}.`); args.set(key.slice(2), value) } return args }
const fixedQuestion = (item, position, quiz = false) => ({ ...(quiz ? { questionType: 'multiple_choice' } : {}), prompt: item[0], explanation: item[3], points: 1, position, status: 'active', choices: item[1].map((text, index) => ({ text, isCorrect: index === item[2], position: index + 1 })) })
export function validateQuestions(label, questions, expected) { const failures = []; if (questions.length !== expected) failures.push(`${label} must have ${expected} questions.`); for (const question of questions) { if (question.choices.length !== 4) failures.push(`${label} question ${question.position} needs four choices.`); if (question.choices.filter((choice) => choice.isCorrect).length !== 1) failures.push(`${label} question ${question.position} needs one answer.`); if (new Set(question.choices.map((choice) => choice.text.trim().toLowerCase())).size !== 4) failures.push(`${label} question ${question.position} has duplicate choices.`); if (question.explanation.trim().length < 15) failures.push(`${label} question ${question.position} lacks explanation.`) } return failures }
const hasRawHtml = (value) => /<\/?[a-z][^>]*>/iu.test(JSON.stringify(value))

function runGeneratorGate() { const entry = fileURLToPath(new URL('../node_modules/vitest/vitest.mjs', import.meta.url)); const result = spawnSync(process.execPath, [entry, 'run', 'tests/logical-reasoning-generators.test.ts'], { stdio: 'inherit', shell: false }); if (result.status !== 0) throw new Error('The 10,000-question logical-reasoning gate failed.') }

export async function publishLogicalReasoning({ baseUrl, cookie: initialCookie, email, password, skipGeneratorGate = false }) {
  if (!skipGeneratorGate) runGeneratorGate()
  let cookie = initialCookie ?? null
  async function request(path, options = {}) { const headers = new Headers(options.headers); headers.set('accept', 'application/json'); if (cookie !== null) headers.set('cookie', cookie); if (options.body !== undefined) headers.set('content-type', 'application/json'); if (options.method !== undefined && options.method !== 'GET') headers.set('x-cse-admin-csrf', csrf); const response = await fetch(`${baseUrl}${path}`, { ...options, headers }); const setCookie = response.headers.get('set-cookie'); if (setCookie !== null) cookie = setCookie.split(';')[0]; const text = await response.text(); let body; try { body = JSON.parse(text) } catch { throw new Error(`${options.method ?? 'GET'} ${path} returned non-JSON status ${response.status}.`) } if (!response.ok || body.success !== true) throw new Error(`${options.method ?? 'GET'} ${path} failed: ${JSON.stringify(body)}`); return body.data }
  if (cookie === null) { if (email === undefined || password === undefined) throw new Error('Pass --cookie, or --email with --password or CSE_LOGICAL_REASONING_ADMIN_PASSWORD.'); await request('/api/auth/login', { method: 'POST', body: JSON.stringify({ email, password }) }) }
  const dashboard = await request('/api/admin/dashboard'); const courseId = dashboard.cseProfessional?.id; if (courseId === undefined) throw new Error('CSE Professional was not found.')
  let detail = await request(`/api/admin/courses/${courseId}`); const numerical = findUniqueBySlug(detail.subjects, 'numerical-ability', 'Numerical Ability subject'); if (numerical === null) throw new Error('Numerical Ability was not found.')
  const subjectPlan = planAnalyticalSubject(detail.subjects, numerical.position)
  let subject = subjectPlan.existing
  if (subject === null) subject = (await request(`/api/admin/courses/${courseId}/subjects`, { method: 'POST', body: JSON.stringify({ title: 'Analytical Ability', slug: subjectSlug, description: subjectDescription, position: subjectPlan.requiredPosition, status: 'draft' }) })).subject
  else if (subject.status !== 'published') subject = (await request(`/api/admin/subjects/${subject.id}`, { method: 'PATCH', body: JSON.stringify({ title: 'Analytical Ability', description: subjectDescription, position: subjectPlan.requiredPosition, status: 'draft', updatedAt: subject.updatedAt }) })).subject
  let topic = findUniqueBySlug(subject.topics, topicSlug, 'Logical Reasoning Fundamentals topic')
  if (topic === undefined) topic = (await request(`/api/admin/subjects/${subject.id}/topics`, { method: 'POST', body: JSON.stringify({ title: 'Logical Reasoning Fundamentals', slug: topicSlug, description: topicDescription, position: 1, status: 'draft' }) })).topic
  else if (topic.status !== 'published') topic = (await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ title: 'Logical Reasoning Fundamentals', description: topicDescription, position: 1, status: 'draft', updatedAt: topic.updatedAt }) })).topic

  for (const spec of lessonSpecs) {
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === subjectSlug)?.topics.find((item) => item.slug === topicSlug); if (topic === undefined) throw new Error('Topic disappeared during creation.')
    let lesson = topic.lessons.find((item) => item.slug === spec.slug)
    if (lesson === undefined) lesson = (await request(`/api/admin/topics/${topic.id}/lessons`, { method: 'POST', body: JSON.stringify({ title: spec.title, slug: spec.slug, lessonType: spec.lessonType, summary: `${spec.title} for CSE logical-reasoning practice.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft' }) })).lesson
    else if (lesson.lessonType !== spec.lessonType) throw new Error(`${spec.slug} has stored type ${lesson.lessonType}; expected ${spec.lessonType}.`)
    else if (lesson.status !== 'published') lesson = (await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ title: spec.title, summary: `${spec.title} for CSE logical-reasoning practice.`, estimatedMinutes: spec.minutes, position: spec.position, isPreview: false, requiresPrevious: true, status: 'draft', updatedAt: lesson.updatedAt }) })).lesson
    const currentBlocks = await request(`/api/admin/lessons/${lesson.id}/blocks`)
    for (const [index, block] of blocksFor(spec.slug).entries()) { const position = index + 1; const existing = currentBlocks.blocks.find((item) => item.position === position); await request(existing === undefined ? `/api/admin/lessons/${lesson.id}/blocks` : `/api/admin/lesson-blocks/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify({ ...block, position }) }) }
    const generatorSlug = generatedByLesson[spec.slug]
    if (generatorSlug !== undefined) await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ title: spec.title, instructions: 'Answer five generated questions using only the stated facts and logical rules.', passingScore: 60, questionCount: 5, maximumAttempts: null, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft', questionSource: 'generated', generatorSlug, generatorVersion: 1, difficulty: { easy: 2, medium: 2, hard: 1 } }) })
    if (spec.slug === 'mixed-logical-reasoning-practice') { const saved = await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ title: spec.title, instructions: 'Answer eight fixed questions using only the supplied information.', passingScore: 60, questionCount: 8, maximumAttempts: null, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft', questionSource: 'fixed' }) }); const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); for (const [index, item] of mixedQuestions.entries()) { const input = fixedQuestion(item, index + 1); const existing = current.questions.find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/practice-sets/${saved.practiceSet.id}/questions` : `/api/admin/practice-questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
    if (spec.slug === 'logical-reasoning-fundamentals-topic-quiz') { const saved = await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ title: spec.title, description: 'A 15-question educational quiz; these are not official CSC questions.', quizType: 'topic', passingScore: 70, timeLimitMinutes: null, maximumAttempts: null, shuffleQuestions: false, shuffleChoices: false, showExplanations: true, status: lesson.status === 'published' ? 'published' : 'draft' }) }); const current = await request(`/api/admin/lessons/${lesson.id}/quiz`); for (const [index, item] of quizQuestions.entries()) { const input = fixedQuestion(item, index + 1, true); const existing = current.questions.find((question) => question.position === input.position); const body = existing === undefined ? input : { ...input, updatedAt: existing.updatedAt, choices: input.choices.map((choice) => ({ ...choice, id: existing.choices.find((old) => old.position === choice.position)?.id })) }; await request(existing === undefined ? `/api/admin/quizzes/${saved.quiz.id}/questions` : `/api/admin/questions/${existing.id}`, { method: existing === undefined ? 'POST' : 'PATCH', body: JSON.stringify(body) }) } }
  }

  const failures = []; const registry = await request('/api/admin/practice-generators'); const registered = new Set(registry.generators.map((item) => `${item.slug}@${item.version}`)); for (const slug of ['statement-classification', ...Object.values(generatedByLesson)]) if (!registered.has(`${slug}@1`)) failures.push(`Generator ${slug}@1 is not registered.`)
  detail = await request(`/api/admin/courses/${courseId}`); const analyticalMatches = detail.subjects.filter((item) => item.slug === subjectSlug); subject = analyticalMatches[0]; topic = subject?.topics.find((item) => item.slug === topicSlug)
  if (analyticalMatches.length !== 1 || subject === undefined) failures.push('Exactly one Analytical Ability subject is required.'); else if (subject.position !== numerical.position + 1) failures.push('Analytical Ability is not immediately after Numerical Ability.')
  if (topic === undefined) failures.push('Logical Reasoning Fundamentals is missing.'); else { if (topic.position !== 1 || topic.description !== topicDescription) failures.push('Topic metadata is invalid.'); if (topic.lessons.length !== 12 || new Set(topic.lessons.map((item) => item.position)).size !== 12) failures.push('Topic must have exactly 12 uniquely positioned lessons.'); for (const spec of lessonSpecs) { const lesson = topic.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) { failures.push(`Missing ${spec.slug}.`); continue } if (lesson.lessonType !== spec.lessonType || lesson.requiresPrevious !== true || lesson.isPreview !== false) failures.push(`${spec.slug} has invalid type or locking.`); const blocks = await request(`/api/admin/lessons/${lesson.id}/blocks`); const expectedBlocks = blocksFor(spec.slug).length; if (blocks.blocks.length !== expectedBlocks || blocks.blocks.length < 9 || hasRawHtml(blocks.blocks)) failures.push(`${spec.slug} has invalid teaching blocks.`); const generatorSlug = generatedByLesson[spec.slug]; if (generatorSlug !== undefined) { const set = (await request(`/api/admin/lessons/${lesson.id}/practice-set`)).practiceSet; if (set?.questionSource !== 'generated' || set.generator?.slug !== generatorSlug || set.generator?.version !== 1 || set.generator?.difficulty.easy !== 2 || set.generator?.difficulty.medium !== 2 || set.generator?.difficulty.hard !== 1 || set.questionCount !== 5 || set.passingScore !== 60 || set.maximumAttempts !== null || set.showExplanations !== true) failures.push(`${spec.slug} has invalid generated configuration.`) } if (spec.slug === 'mixed-logical-reasoning-practice') failures.push(...validateQuestions('Mixed practice', (await request(`/api/admin/lessons/${lesson.id}/practice-set`)).questions, 8)); if (spec.slug === 'logical-reasoning-fundamentals-topic-quiz') failures.push(...validateQuestions('Topic quiz', (await request(`/api/admin/lessons/${lesson.id}/quiz`)).questions, 15)) } }
  if (failures.length > 0) throw new Error(`Validation failed; nothing was published:\n- ${failures.join('\n- ')}`)

  const rollback = []
  try {
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === subjectSlug)?.topics.find((item) => item.slug === topicSlug)
    for (const spec of lessonSpecs) { const lesson = topic?.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.slug} disappeared.`); const generatorSlug = generatedByLesson[spec.slug]; if (generatorSlug !== undefined) { const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); if (current.practiceSet.status !== 'published') { const config = { title: spec.title, instructions: 'Answer five generated questions using only the stated facts and logical rules.', passingScore: 60, questionCount: 5, maximumAttempts: null, showExplanations: true, questionSource: 'generated', generatorSlug, generatorVersion: 1, difficulty: { easy: 2, medium: 2, hard: 1 } }; await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.practiceSet.status }) })) } } else if (spec.lessonType === 'practice') { const current = await request(`/api/admin/lessons/${lesson.id}/practice-set`); if (current.practiceSet.status !== 'published') { const config = { title: spec.title, instructions: 'Answer eight fixed questions using only the supplied information.', passingScore: 60, questionCount: 8, maximumAttempts: null, showExplanations: true, questionSource: 'fixed' }; await request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/practice-set`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.practiceSet.status }) })) } } else if (spec.lessonType === 'quiz') { const current = await request(`/api/admin/lessons/${lesson.id}/quiz`); if (current.quiz.status !== 'published') { const config = { title: spec.title, description: 'A 15-question educational quiz; these are not official CSC questions.', quizType: 'topic', passingScore: 70, timeLimitMinutes: null, maximumAttempts: null, shuffleQuestions: false, shuffleChoices: false, showExplanations: true }; await request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ ...config, status: 'published' }) }); rollback.push(() => request(`/api/admin/lessons/${lesson.id}/quiz`, { method: 'PUT', body: JSON.stringify({ ...config, status: current.quiz.status }) })) } } }
    detail = await request(`/api/admin/courses/${courseId}`); topic = detail.subjects.find((item) => item.slug === subjectSlug)?.topics.find((item) => item.slug === topicSlug)
    for (const spec of lessonSpecs) { const lesson = topic?.lessons.find((item) => item.slug === spec.slug); if (lesson === undefined) throw new Error(`${spec.slug} disappeared.`); if (lesson.status !== 'published') { await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: lesson.updatedAt }) }); rollback.push(async () => { const refreshed = await request(`/api/admin/courses/${courseId}`); const current = refreshed.subjects.find((item) => item.slug === subjectSlug)?.topics.find((item) => item.slug === topicSlug)?.lessons.find((item) => item.id === lesson.id); if (current !== undefined) await request(`/api/admin/lessons/${lesson.id}`, { method: 'PATCH', body: JSON.stringify({ status: lesson.status, updatedAt: current.updatedAt }) }) }) } }
    detail = await request(`/api/admin/courses/${courseId}`); subject = detail.subjects.find((item) => item.slug === subjectSlug); topic = subject?.topics.find((item) => item.slug === topicSlug); if (subject === undefined || topic === undefined) throw new Error('Subject or topic disappeared before publication.')
    if (topic.status !== 'published') { const prior = topic.status; await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: topic.updatedAt }) }); rollback.push(async () => { const fresh = await request(`/api/admin/courses/${courseId}`); const current = fresh.subjects.find((item) => item.slug === subjectSlug)?.topics.find((item) => item.slug === topicSlug); if (current !== undefined) await request(`/api/admin/topics/${topic.id}`, { method: 'PATCH', body: JSON.stringify({ status: prior, updatedAt: current.updatedAt }) }) }) }
    detail = await request(`/api/admin/courses/${courseId}`); subject = detail.subjects.find((item) => item.slug === subjectSlug); if (subject === undefined) throw new Error('Subject disappeared before publication.'); if (subject.status !== 'published') { const prior = subject.status; await request(`/api/admin/subjects/${subject.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'published', updatedAt: subject.updatedAt }) }); rollback.push(async () => { const fresh = await request(`/api/admin/courses/${courseId}`); const current = fresh.subjects.find((item) => item.slug === subjectSlug); if (current !== undefined) await request(`/api/admin/subjects/${subject.id}`, { method: 'PATCH', body: JSON.stringify({ status: prior, updatedAt: current.updatedAt }) }) }) }
  } catch (error) { await rollbackStatusChanges(rollback, (rollbackError) => console.error(`Rollback failed: ${rollbackError instanceof Error ? rollbackError.message : String(rollbackError)}`)); throw error }
  return { courseId, subjectSlug, topicSlug, lessons: lessonSpecs.length }
}

async function main() { const args = parseArgs(); if (args.get('confirm') !== CONFIRMATION) throw new Error(`Pass --confirm ${CONFIRMATION} to continue.`); const result = await publishLogicalReasoning({ baseUrl: args.get('base-url') ?? 'http://127.0.0.1:5173', cookie: args.get('cookie'), email: args.get('email'), password: args.get('password') ?? process.env.CSE_LOGICAL_REASONING_ADMIN_PASSWORD }); console.log(`Created, validated, and published ${result.topicSlug} with ${result.lessons} lessons.`) }

if (process.argv[1] !== undefined && fileURLToPath(import.meta.url) === fileURLToPath(new URL(`file:///${process.argv[1].replaceAll('\\', '/')}`))) main().catch((error) => { console.error(error instanceof Error ? error.message : String(error)); process.exitCode = 1 })
