/**
 * Official future CSE targets, stored as ISO date-only values (YYYY-MM-DD).
 * Keep empty until an authoritative date is approved for PasaWise.
 */
export const configuredCseExamDates: readonly string[] = []
